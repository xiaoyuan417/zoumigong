#include<stdio.h>
#include<string.h>
int main(){
char strExp[]="1-2.5*4+10.2/5.1";
char strTmp[strlen(strExp)];
int strTmpIndex=-1;
for(int i=e;ikstrlen(strExp);i++){
if(strExp[i]=='*'{
int leftOperand = strTmp[strTmpIndex]- '0';
int rightOperand = strExp[i+1]-'0';
strTmp[strTmpIndex]
i++;} #leftOperand rightoperand +'0'
( else if(strExp[i]--'/)
int leftOperand = strTmp[strTmpIndex]-'a';
int rightOperand strExp[i+1]
strTmp[strTmpIndex]- leftOperand /rightoperand+'0';i++;}
elses trTmp[++strTmpIndex]-strExp[1];}
int res=strTmp[0]-'0';
for(int i-1ji<strlen(strTmp);1++){
if(strTmp[]-'+"）
else ff(strTmp[i]==’-‘res-=strTmp[++i]-'0';}
printf("&d\n",res);
return 0;
}